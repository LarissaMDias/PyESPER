[pytest]
testpaths = PyESPER/tests
markers =
    slow: end-to-end tests that run the full estimation pipeline (tens of seconds).
filterwarnings =
    ignore:overflow encountered in exp:RuntimeWarning
